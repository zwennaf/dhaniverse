# Dhaniverse Server Environment Variables

# Database Configuration
# MongoDB Atlas connection string (required)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dhaniverse?retryWrites=true&w=majority

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Server Configuration
NODE_ENV=development
DENO_ENV=development  # Used by Deno applications
PORT=8000

ALLOWED_ORIGINS=
# MongoDB Atlas connection string format:
# mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/dhaniverse?retryWrites=true&w=majority
#
# Setup instructions:
# 1. Create MongoDB Atlas account at https://cloud.mongodb.com/
# 2. Create a cluster and database user
# 3. Get connection string from "Connect" button
# 4. Replace <password> with your database user password
# 5. Database name should be "dhaniverse"